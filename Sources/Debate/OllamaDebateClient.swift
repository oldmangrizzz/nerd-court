import Foundation

// MARK: - Legacy shim (retired)
//
// The Delta-hosted Ollama Max harness has been retired. Inference now routes
// exclusively through ollama.com's cloud API via `OllamaCloudClient`.
// This file is intentionally empty of behavior — callers must depend on
// `OllamaCloudClient` directly.
